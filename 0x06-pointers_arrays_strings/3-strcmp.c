#include "main.h"

/**
 * _strcmp - fn to compare two string
 * @s1: first string to compare
 * @s2: second string to compare
 *
 * Return: {int} the compareison result
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
