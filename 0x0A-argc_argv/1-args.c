#include "main.h"

/**
 * main - Entry Point
 * @argc: script arguments count
 * @argv: script passed arguments
 *
 * Return: {int}
 */
int main(int argc, char *argv[])
{
	if (argv[0])
		printf("%d\n", argc);

	return (0);
}
