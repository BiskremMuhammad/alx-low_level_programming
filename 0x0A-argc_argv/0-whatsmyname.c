#include "main.h"

/**
 * main - entry point
 * @argc: script arguments count
 * @argv: script passed arguments
 *
 * Return: {int}
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);

	return (0);
}
