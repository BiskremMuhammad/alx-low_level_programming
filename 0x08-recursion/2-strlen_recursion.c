#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - fn to return string length
 * @s: the string
 *
 * Return: the length of the string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
