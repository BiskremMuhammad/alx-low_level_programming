#include "main.h"
#include <math.h>

/**
 * _pow_recursion - fn to return result of x ^ y
 * @x: base number
 * @y: power
 *
 * Return: {int} result of x ^ y
 */
int _pow_recursion(int x, int y)
{
	return (pow(x, y));
}
