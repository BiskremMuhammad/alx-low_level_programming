#include "main.h"
#include <string.h>

/**
 * _strlen - fn to get length of a string
 * @s: the string pointer
 *
 * Return: {int} the length of the string
 */
int _strlen(char *s)
{
	return (strlen(*s));
}
