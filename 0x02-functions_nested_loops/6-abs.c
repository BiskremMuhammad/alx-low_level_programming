#include "main.h"
#include <stdlib.h>

/**
 * _abs - fn to get the absolute value of a number
 * @n: the number
 *
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	return (abs(n));
}
